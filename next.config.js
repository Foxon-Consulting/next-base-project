/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export", // before i had "standalone" instead of export. I don't know wyh. Maybe using this template in the future i will discover it.
};

module.exports = nextConfig;
